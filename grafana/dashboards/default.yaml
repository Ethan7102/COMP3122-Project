apiVersion: 1

providers:
  - name: applicationHealthMonitoring    # A uniquely identifiable name for the provider
    folder: dashboards # The folder where to place the dashboards
    type: file
    options:
      path:  /etc/grafana/provisioning/dashboards/